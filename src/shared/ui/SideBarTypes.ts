export type DepartmentType = {
  id: number;
  name: string;
  url: string;
};

export type MenuItemsType = {
  name: string;
  department_id: number;
  order: number;
  url: string;
  icon: string;
};
